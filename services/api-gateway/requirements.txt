Flask==3.0.3
flask-jwt-extended==4.6.0
Flask-Limiter[redis]==3.7.0
flask-cors==4.0.1
requests==2.32.3
redis==5.0.6
python-dotenv==1.0.1
gunicorn==22.0.0

# Postgres access for chat history (gateway shares the auth-service DB)
psycopg2-binary==2.9.9
SQLAlchemy==2.0.32

# Realtime: SocketIO backed by Redis message queue (DB 2)
# async_mode="threading" — no eventlet; standard threads + Redis queue for fan-out
flask-socketio==5.3.7
python-socketio[redis]==5.11.3

# Testing
pytest==8.2.2
pytest-flask==1.3.0
responses==0.25.3
sentry-sdk[flask]>=2.0,<3.0
